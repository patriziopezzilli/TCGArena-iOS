# Contributing to TCG Arena iOS App

Grazie per il tuo interesse nel contribuire a TCG Arena! Questo documento fornisce le linee guida per contribuire al progetto.

## 🚀 Come Contribuire

### 1. Fork e Clone
```bash
git clone https://github.com/patriziopezzilli/TCGArena-iOS.git
cd "TCG Arena - iOS App"
```

### 2. Crea un Branch
```bash
git checkout -b feature/nome-della-tua-feature
```

### 3. Sviluppa
- Segui le convenzioni di codice Swift
- Aggiungi test per nuove funzionalità
- Assicurati che tutti i test passino
- Aggiorna la documentazione se necessario

### 4. Commit
```bash
git add .
git commit -m "feat: descrizione della feature aggiunta"
```

Usa i seguenti prefissi per i commit:
- `feat:` per nuove funzionalità
- `fix:` per correzioni di bug
- `docs:` per aggiornamenti alla documentazione
- `style:` per modifiche di stile/formattazione
- `refactor:` per refactoring del codice
- `test:` per aggiunta/modifica di test

### 5. Push e Pull Request
```bash
git push origin feature/nome-della-tua-feature
```

Crea una Pull Request su GitHub descrivendo i cambiamenti effettuati.

## 📋 Requisiti per le Pull Request

- [ ] Il codice compila senza errori
- [ ] Tutti i test passano
- [ ] Il codice segue le convenzioni Swift
- [ ] La documentazione è aggiornata
- [ ] I cambiamenti sono testati su dispositivi/simulatori

## 🏗️ Setup Ambiente di Sviluppo

### Prerequisiti
- Xcode 14.0+
- iOS 15.0+ SDK
- Swift 5.7+

### Configurazione
1. Apri `TCG Arena.xcodeproj` in Xcode
2. Configura Firebase aggiungendo `GoogleService-Info.plist`
3. Builda ed esegui l'app

## 🐛 Segnalazione Bug

Usa il template delle Issues di GitHub per segnalare bug, includendo:
- Descrizione del problema
- Passi per riprodurre
- Versione iOS
- Modello dispositivo
- Screenshots se applicabile

## 💡 Suggerimenti per Nuove Features

Prima di implementare nuove funzionalità, crea una Issue per discutere l'idea con il team.

## 📞 Contatti

Per domande o chiarimenti, apri una Issue o contatta il maintainer del progetto.

Grazie per contribuire a TCG Arena! 🎮